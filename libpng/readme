modif Makefile :
INCLUDES  += -I/home/ad/libC/libpng/include
LDFLAGS   += -L/home/ad/libC/libpng/lib
LIBS      += -lpng -lz

.so non trouvés (*** à remplacer correctement) :
cp libpng/lib/***.so*** /usr/lib
cp libpng/lib/***.so*** /usr/lib32

includes dans le programme :
#include "png.h"
#include "zlib.h"

